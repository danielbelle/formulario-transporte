{
    "$schema": "https://railway.com/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "composer install --optimize-autoloader --no-dev && npm install && npm run build && php artisan optimize && chmod -R 775 storage bootstrap/cache"
    },
    "deploy": {
        "runtime": "V2",
        "startCommand": "php server.php",
        "limitOverride": {
            "containers": {
                "cpu": 0.5,
                "memoryBytes": 3000000000
            }
        },
        "sleepApplication": false,
        "multiRegionConfig": {
            "us-east4-eqdc4a": {
                "numReplicas": 1
            }
        },
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}
